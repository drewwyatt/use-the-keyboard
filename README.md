# Use The Keyboard

Chrome extension to encourage using keyboard shortcuts for webpages that offer them.

## Build and run

```
yarn start
```

Runs `webpack --watch`

```
yarn build
```

Runs `webpack`